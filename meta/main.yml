galaxy_info:
    role_name: kypo-user-access
    author: KYPO Team
    description: Distribution of user SSH public key to authorized keys.
    licence: MIT
    min_ansible_version: 2.3.3
    platforms:
        - name: Debian
          versions:
              - all
        - name: Ubuntu
          versions:
              - all

